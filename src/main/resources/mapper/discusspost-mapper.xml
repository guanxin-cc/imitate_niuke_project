<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.DiscussPostMapper">

<!--    <sql id="selectFields">-->
<!--        id, user_id, title, content, type, status, create_time, comment_count, score-->
<!--    </sql>-->

<!--    <select id="selectDiscussPosts" resultType="DiscussPost">-->
<!--        select <include refid="selectFields"/>-->
<!--        from discuss_post-->
<!--&#45;&#45;      帖子不能被拉黑-->
<!--        where status != 2-->
<!--&#45;&#45;         test 属性里写判断逻辑，这里表示 “当 不等于 0 时，才执行里面的 SQL”-->
<!--        <if test="userId!=0">-->
<!--            and user_id = #{userId}-->
<!--        </if>-->
<!--&#45;&#45;       0 普通 1 置顶 1排到前面 /  在类型倒序的前提下， 创建时间倒序，新的帖子在前面-->
<!--        order by type desc, create_time desc-->
<!--&#45;&#45;      LIMIT 0, 10：从第 1 条数据开始，取 10 条 → 第 1-10 条-->
<!--        &#45;&#45;左开右闭-->
<!--        limit #{offset}, #{limit}-->
<!--    </select>-->

<!--    <select id="selectDiscussPostRows" resultType="int">-->
<!--        select count(id)-->
<!--        from discuss_post-->
<!--        where status != 2-->
<!--        <if test="userId!=0">-->
<!--            and user_id = #{userId}-->
<!--        </if>-->
<!--    </select>-->
    <sql id="selectFields">id,user_id,title,content,type,status,create_Time,comment_count,score</sql>

    <select id="selectDiscussPosts" resultType="DiscussPost">
        select <include refid="selectFields"/>
        from discuss_post
        where status != 2
        <if test="userId!=0">
            and user_id = #{userId}
        </if>
        limit #{offset},#{limit}
    </select>
<!--    很好奇为什么 不写resultTypeMybatis就不知道把 count(*)映射成什么类型-->
    <select id="selectDiscussPostRows" resultType="int">
        select count(*)
        from discuss_post
        where status != 2
        <if test="userId!=0">
            and user_id = #{userId}
        </if>
    </select>
</mapper>

